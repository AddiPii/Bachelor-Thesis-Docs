# Bachelor-Thesis-Docs
Bachelor's thesis documentation for AddiPi – a microservice-based 3D printer management system.

# TexStudio Config

LaTeX:
```bash
latexmk -pdf -silent -synctex=1 "dyplomWFA"
```

PdfLaTeX
```bash
pdflatex -shell-escape -synctex=1 -interaction=nonstopmode %.tex
```

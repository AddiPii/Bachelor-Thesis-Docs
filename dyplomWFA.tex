%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Praca inżynierska — System zarządzania pracą drukarki 3D
% Oliwer Urbaniak, Uniwersytet Wrocławski
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\documentclass[a4paper,12pt,reqno]{article}
%----------------------------------------------------------
\usepackage{amsfonts}
\usepackage{graphicx}
\usepackage{geometry}
\usepackage{color}
\usepackage{amssymb,amsmath}
\usepackage{polski}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{caption}

% --- NOWE MARGINESY (Zgodnie ze zdjęciem) ---
\geometry{
	left=35mm,  % Średnia wartość z zakresu 35-40mm
	right=25mm,
	top=25mm,
	bottom=25mm
}

\usepackage{wrapfig}
\usepackage{lipsum}  
\usepackage{listings}
\usepackage[polish]{babel}
\usepackage[toc,page]{appendix}
\usepackage{url}
\usepackage{enumitem}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{xcolor}
\usepackage{makecell}
\usepackage{float}

\usepackage{tabularx}
\usepackage{xltabular}

% --- NOWY KRÓJ PISMA I INTERLINIA ---
\usepackage{mathptmx} % Zamiennik Times New Roman dla LaTeX
\usepackage{setspace}
\setstretch{1.5}     % Interlinia 1,5

% --- ROZMIARY TYTUŁÓW (Sekcji) ---
\usepackage{titlesec}
\titleformat{\section}{\normalfont\fontsize{14}{17}\bfseries}{\thesection}{1em}{}
\titleformat{\subsection}{\normalfont\fontsize{14}{17}\bfseries}{\thesubsection}{1em}{}


\renewcommand{\appendixtocname}{Załączniki}   % Nazwa w spisie treści
\renewcommand{\appendixpagename}{Załączniki}  % Nazwa na osobnej stronie nagłówkowej
\renewcommand{\appendixname}{Załącznik}        % Nazwa pojedynczego załącznika

\usepackage{hyperref}
\hypersetup{
	colorlinks=true,      % Włącza kolorowanie tekstu zamiast ramek
	linkcolor=black,      % Kolor linków wewnętrznych (np. w spisie treści) - zmień na navy blue lub zostaw black
	filecolor=magenta,    % Kolor linków do plików lokalnych
	urlcolor=blue,        % Kolor tradycyjnych linków internetowych (URL)
	citecolor=blue       % Kolor odnośników do bibliografii
}

\definecolor{codegreen}{rgb}{0.5, 0.09, 0.09}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.94,0.94,0.94}
\definecolor{gray}{rgb}{0,0.6,0}
\definecolor{codeblue}{rgb}{0,0,0.8}

% Kolory dla eleganckiego, odciętego bloku kodu
\definecolor{vslBackground}{HTML}{F8F9FA}  % Bardzo jasny szary (odcina kod od białej strony)
\definecolor{vslBorder}{HTML}{E5E7EB}      % Delikatna, nowoczesna ramka wokół kodu
\definecolor{vslText}{HTML}{111827}        % Ciemnoszary, czytelny tekst kodu
\definecolor{vslKeyword}{HTML}{0000FF}     % Słowa kluczowe (if, const)
\definecolor{vslString}{HTML}{A31515}      % Stringi
\definecolor{vslComment}{HTML}{008000}     % Komentarze
\definecolor{vslNumber}{HTML}{098658}      % Liczby
\definecolor{vslType}{HTML}{267F99}        % Typy i Interfejsy
\definecolor{vslLineNum}{HTML}{9CA3AF}     % Stonowane, szare numery linii

% Kolory dla eleganckiego bloku kodu (wspólne z tsStyle)
\definecolor{pyBackground}{HTML}{F8F9FA}   % Jasnoszare tło
\definecolor{pyBorder}{HTML}{E5E7EB}       % Delikatna ramka
\definecolor{pyText}{HTML}{111827}         % Główny tekst kodu
\definecolor{pyKeyword}{HTML}{0000FF}      % Słowa kluczowe (def, if, class)
\definecolor{pyBuiltin}{HTML}{267F99}      % Wbudowane funkcje/typy (print, len, int)
\definecolor{pyString}{HTML}{A31515}       % Stringi
\definecolor{pyComment}{HTML}{008000}      % Komentarze (#)
\definecolor{pyNumber}{HTML}{098658}       % Liczby
\definecolor{pyLineNum}{HTML}{9CA3AF}      % Numery linii

% Kolory dla eleganckiego bloku kodu (wspólne z ts i py Style)
\definecolor{jsBackground}{HTML}{F8F9FA}   % Jasnoszare tło
\definecolor{jsBorder}{HTML}{E5E7EB}       % Delikatna ramka
\definecolor{jsText}{HTML}{111827}         % Główny tekst kodu
\definecolor{jsKeyword}{HTML}{0000FF}      % Słowa kluczowe (const, let, function)
\definecolor{jsBuiltin}{HTML}{267F99}      % Wbudowane obiekty i funkcje (console, Math, JSON)
\definecolor{jsString}{HTML}{A31515}       % Stringi
\definecolor{jsComment}{HTML}{008000}      % Komentarze
\definecolor{jsNumber}{HTML}{098658}       % Liczby
\definecolor{jsLineNum}{HTML}{9CA3AF}      % Numery linii


\lstdefinestyle{mystyle}{
	backgroundcolor=\color{backcolour},
	commentstyle=\color{codegreen},
	keywordstyle=\color{codeblue},
	numberstyle=\tiny\color{codegray},
	stringstyle=\color{codepurple},
	basicstyle=\footnotesize\fontfamily{cmtt}\selectfont,
	breakatwhitespace=false,
	breaklines=true,
	captionpos=b,
	keepspaces=true,
	numbers=left,
	numbersep=5pt,
	showspaces=false,
	showstringspaces=false,
	showtabs=false,
	tabsize=2
}

\lstdefinestyle{jsonStyle}{
	backgroundcolor=\color{backcolour},
	commentstyle=\color{codegreen},
	keywordstyle=\color{codeblue},
	numberstyle=\tiny\color{codegray},
	stringstyle=\color{codepurple},
	basicstyle=\footnotesize\fontfamily{cmtt}\selectfont,
	breaklines=true,
	captionpos=b,
	keepspaces=true,
	numbers=left,
	numbersep=5pt,
	showspaces=false,
	showstringspaces=false,
	showtabs=false,
	tabsize=2
}

% Definicja dodatkowych słów kluczowych dla TypeScript
\lstdefinelanguage{TypeScript}{
	keywords={abstract, as, break, case, catch, class, const, continue, debugger, declare, default, delete, do, else, enum, export, extends, false, finally, for, from, function, get, if, implements, import, in, instanceof, interface, let, module, namespace, new, null, of, package, private, protected, public, readonly, require, return, set, static, super, switch, this, throw, true, try, type, typeof, var, void, while, with, yield},
	% Typy i klasy
	keywords=[2]{string, number, boolean, any, unknown, never, void, null, undefined, object, Promise, Observable},
	morecomment=[l]{//},
	morecomment=[s]{/*}{*/},
	morestring=[b]",
	morestring=[b]',
	morestring=[b]`,
	literate={0}{{\textcolor{vslNumber}{0}}}{1}
	{1}{{\textcolor{vslNumber}{1}}}{1}
	{2}{{\textcolor{vslNumber}{2}}}{1}
	{3}{{\textcolor{vslNumber}{3}}}{1}
	{4}{{\textcolor{vslNumber}{4}}}{1}
	{5}{{\textcolor{vslNumber}{5}}}{1}
	{6}{{\textcolor{vslNumber}{6}}}{1}
	{7}{{\textcolor{vslNumber}{7}}}{1}
	{8}{{\textcolor{vslNumber}{8}}}{1}
	{9}{{\textcolor{vslNumber}{9}}}{1}
}

\lstdefinestyle{tsStyle}{
	language=TypeScript,
	backgroundcolor=\color{backcolour},
	commentstyle=\color{vslComment}\itshape,
	keywordstyle=\color{vslKeyword}\bfseries,
	keywordstyle=[2]\color{vslType}, 
	stringstyle=\color{vslString},
	numberstyle=\tiny\color{vslLineNum},
	basicstyle=\footnotesize\fontfamily{cmtt}\selectfont\color{vslText},
	breaklines=true,
	captionpos=b,
	keepspaces=true,
	numbers=left,
	numbersep=8pt,
	showspaces=false,
	showstringspaces=false,
	showtabs=false,
	tabsize=2,
	% --- NOWE PARAMETRY RAMKI I ODSTĘPÓW ---
	framesep=5pt,               % Odstęp ramki od kodu w środku
	xleftmargin=3pt,           % Wcięcie całego bloku z lewej strony
	xrightmargin=5pt            % Wcięcie bloku z prawej strony
}

\lstdefinelanguage{PythonCustom}{
	% Główne słowa kluczowe
	keywords={False, None, True, and, as, assert, async, await, break, class, continue, def, del, elif, else, except, finally, for, from, global, if, import, in, is, lambda, nonlocal, not, or, pass, raise, return, try, while, with, yield},
	% Wbudowane funkcje i typy danych (druga grupa)
	keywords=[2]{abs, all, any, bin, bool, bytes, chr, dict, dir, divmod, enumerate, eval, exec, filter, float, format, hash, hex, id, input, int, isinstance, len, list, map, max, min, next, object, open, ord, pow, print, range, repr, reversed, round, set, slice, sorted, str, sum, tuple, type, zip, self, cls},
	sensitive=true,
	morecomment=[l]{\#},
	morecomment=[s]{"""}{"""},
	morecomment=[s]{'''}{'''},
	morestring=[b]",
	morestring=[b]',
	% Stabilne kolorowanie liczb
	literate={0}{{\textcolor{pyNumber}{0}}}{1}
	{2}{{\textcolor{pyNumber}{2}}}{1}
	{1}{{\textcolor{pyNumber}{1}}}{1}
	{3}{{\textcolor{pyNumber}{3}}}{1}
	{4}{{\textcolor{pyNumber}{4}}}{1}
	{5}{{\textcolor{pyNumber}{5}}}{1}
	{6}{{\textcolor{pyNumber}{6}}}{1}
	{7}{{\textcolor{pyNumber}{7}}}{1}
	{8}{{\textcolor{pyNumber}{8}}}{1}
	{9}{{\textcolor{pyNumber}{9}}}{1}
}

\lstdefinestyle{pyStyle}{
	language=PythonCustom,
	backgroundcolor=\color{pyBackground},
	commentstyle=\color{pyComment}\itshape,
	keywordstyle=\color{pyKeyword}\bfseries,
	keywordstyle=[2]\color{pyBuiltin}, % Kolorowanie wbudowanych funkcji
	stringstyle=\color{pyString},
	numberstyle=\tiny\color{pyLineNum},
	basicstyle=\footnotesize\fontfamily{cmtt}\selectfont\color{pyText},
	breaklines=true,
	captionpos=b,
	keepspaces=true,
	numbers=left,
	numbersep=8pt,
	showspaces=false,
	showstringspaces=false,
	showtabs=false,
	tabsize=4, % Python standardowo używa wcięć na 4 spacje
	% --- PARAMETRY RAMKI I ODSTĘPÓW ---
	framesep=5pt,               % Odstęp ramki od kodu w środku
	xleftmargin=3pt,           % Wcięcie całego bloku z lewej strony
	xrightmargin=5pt            % Wcięcie bloku z prawej strony
}

\lstdefinelanguage{JavaScript}{
	% Główne słowa kluczowe JavaScript
	keywords={async, await, break, case, catch, class, const, continue, debugger, default, delete, do, else, export, extends, false, finally, for, from, function, get, if, import, in, instanceof, let, new, null, of, return, set, static, super, switch, this, throw, true, try, typeof, var, void, while, with, yield},
	% Wbudowane obiekty, globalne zmienne i metody (druga grupa)
	keywords={window, document, console, log, error, warn, Object, Array, String, Number, Boolean, Function, Promise, JSON, Math, Date, RegExp, Map, Set, parseInt, parseFloat, isNaN},
	sensitive=true,
	morecomment=[l]{//},
	morecomment=[s]{/*}{*/},
	morestring=[b]",
	morestring=[b]',
	morestring=[b]`, % Obsługa template literals
	% Stabilne kolorowanie liczb
	literate={0}{{\textcolor{jsNumber}{0}}}{1}
	{1}{{\textcolor{jsNumber}{1}}}{1}
	{2}{{\textcolor{jsNumber}{2}}}{1}
	{3}{{\textcolor{jsNumber}{3}}}{1}
	{4}{{\textcolor{jsNumber}{4}}}{1}
	{5}{{\textcolor{jsNumber}{5}}}{1}
	{6}{{\textcolor{jsNumber}{6}}}{1}
	{7}{{\textcolor{jsNumber}{7}}}{1}
	{8}{{\textcolor{jsNumber}{8}}}{1}
	{9}{{\textcolor{jsNumber}{9}}}{1}
}

\lstdefinestyle{jsStyle}{
	language=JavaScript,
	backgroundcolor=\color{jsBackground},
	commentstyle=\color{jsComment}\itshape,
	keywordstyle=\color{jsKeyword}\bfseries,
	keywordstyle=\color{jsBuiltin}, % Kolorowanie wbudowanych obiektów (morski)
	stringstyle=\color{jsString},
	numberstyle=\tiny\color{jsLineNum},
	basicstyle=\footnotesize\fontfamily{cmtt}\selectfont\color{jsText},
	breaklines=true,
	captionpos=b,
	keepspaces=true,
	numbers=left,
	numbersep=8pt,
	showspaces=false,
	showstringspaces=false,
	showtabs=false,
	tabsize=2, % Standardowe 2 spacje dla JS
	% --- PARAMETRY RAMKI I ODSTĘPÓW ---
	framesep=5pt,               % Odstęp ramki od kodu w środku
	xleftmargin=3pt,           % Wcięcie całego bloku z lewej strony
	xrightmargin=5pt            % Wcięcie bloku z prawej strony
}
 
\lstset{style=mystyle}
\lstset{literate=%
    *{0}{{{\color{gray}0}}}1
    {1}{{{\color{gray}1}}}1
    {2}{{{\color{gray}2}}}1
    {3}{{{\color{gray}3}}}1
    {4}{{{\color{gray}4}}}1
    {5}{{{\color{gray}5}}}1
    {6}{{{\color{gray}6}}}1
    {7}{{{\color{gray}7}}}1
    {8}{{{\color{gray}8}}}1
    {9}{{{\color{gray}9}}}1
}
%------------------------------------------------------------
\begin{document}
	
% ============================================================
%  STRONA TYTUŁOWA
% ============================================================

%\begin{figure}[h]
%	\centering
%		\includegraphics[width=0.40\textwidth]{logo.pdf}
%\end{figure}


\begin{center}

\thispagestyle{empty}

%UNIWERSYTET WROCŁAWSKI\\
\Large 
Uniwersytet Wrocławski\\
Wydział Fizyki i Astronomii\\
\vspace{0.8cm}
\vspace{1.8cm}

\Large Oliwer Urbaniak \\
\vspace{3.2cm}
\Large System zarządzania pracą drukarki 3D w laboratorium studenckim. \\

\end{center}
\vspace{5.5cm}
\begin{flushright}
\large{ Praca inżynierska na kierunku \\Informatyka Stosowana i Systemy Pomiarowe \\}
\vspace{0.5cm}
\large{ Opiekun \\ dr Bartosz Brzostowski}
\end{flushright}
\vspace{2.2cm}

\begin{center}
\large Wrocław, \today
\end{center}

\newpage

% ============================================================
%  SPIS TREŚCI
% ============================================================

\tableofcontents

\newpage
% ============================================================
%  STRESZCZENIE
% ============================================================
\begin{flushleft}
\Large \textbf{Streszczenie}
\end{flushleft}
\vspace{1cm}


Niniejsza praca inżynierska opisuje projekt, implementację oraz wykonanie systemu \textit{AddiPi} - rozproszonego systemu zarządzania pracą drukarki 3D przeznaczonego dla laboratorium studenckiego. System umożliwia użytkownikom przesyłanie plików G-code, planowanie oraz monitorowanie zadań do wydruku, a administratorom - pełną kontrolę nad kolejką i użytkownikami.

Architektura systemu oparta jest na podejściu mikroserwisowym. Wyodrębniono sześć niezależnych serwisów backendowych: uwierzytelniania (Auth Service), zarządzania użytkownikami (User Service), obsługi plików (Files Service), kolejkowania zadań (Queue Service), sterowania drukarką (Printer Service) oraz agenta działającego na urządzeniu Raspberry Pi (AddiPi Agent). Całość uzupełnia aplikacja frontendowa zbudowana w React oraz serwis wideo do podglądu kamery.

Komunikacja między serwisami odbywa się za pośrednictwem protokołu HTTP/REST oraz chmurowej kolejki Azure Service Bus. Dane przechowywane są w Azure Cosmos DB, a pliki G-code w Azure Blob Storage. Sterowanie drukarką realizowane jest przez Azure IoT Hub przy użyciu bezpośrednich wywołań metod (Direct Methods) na Raspberry Pi, na którym działa oprogramowane OctoPrint.

System zrealizowano przy użyciu języków TypeScript (Node.js) i Python. Wdrożenie odbywa się w kontenerach Docker, koordynowanych plikiem Docker Compose. Interfejs użytkownika obsługuje uwierzytelnianie tokenem JWT, przesyłanie plików, planowanie zadań, podgląd kamery w czasie rzeczywistym oraz panel administracyjny.

\vspace{0.3cm}
\textbf{Słowa kluczowe:} mikroserwisy, drukarka 3D, OctoPrint, Raspberry Pi, Azure Iot Hub, Azure Cosmos DB, Docker, TypeScript, Python, REST API, kolejkowanie zadań

\newpage

% ============================================================
%  ROZDZIAŁ 1 — WPROWADZENIE
% ============================================================

\section{Wprowadzenie}

\subsection{Motywacja i cel pracy}



Drukarki 3D stanowią obecnie standardowe wyposażenie laboratoriów akademickich, warsztatów studenckich oraz zakładów produkcyjnych zorientowanych na wytwarzanie przyrostowe. Urządzenia te, pomimo rosnącej dostępności i malejących cen, pozostają zasobem współdzielonym - ich liczba jest zwykle ograniczona, a czas drukowania pojedynczego modelu może wynosić od kilkunastu minut do wielu godzin. W środowisku akademickim oraz przemysłowym rodzi to naturalne problemy organizacyjne: konieczność ręcznego umawiania się na czas druku, brak przejrzystości stanu kolejki oraz niemożność zdalnego monitorowania postępu pracy.

Celem niniejszej pracy jest zaprojektowanie i implementacja systemu \textit{AddiPi}, który automatyzuje proces zarządzania drukarką 3D w laboratorium studenckim. System pozwala użytkownikom na:
\begin{itemize}
	\item przesyłanie plików G-code przez przeglądarkę internetową,
	\item planowanie druków na określony termin lub dodawanie ich do kolejki,
	\item śledzenie postępu druku w czasie rzeczywistym, wraz z podglądem kamery,
	\item otrzymywanie powiadomień o zakończeniu lub błędzie druku.
\end{itemize}


Administratorzy laboratorium zyskują natomiast narzędzia do zarządzania użytkownikami, przeglądania historii wszystkich zadań oraz sterowania kolejką i urządzeniami.

\subsection{Zakres pracy}

Praca obejmuje:
\begin{enumerate}
	\item analizę wymagań systemu i wybór technologii,
	\item projekt architektury mikroserwisowej,
	\item implementację wszystkich komponentów backendowych i frontendowych,
	\item konfigurację infrastruktury chmurowej (Microsoft Azure),
	\item wdrożenie systemu przy użyciu kontenerów Docker,
	\item testy poprawności działania.
\end{enumerate}

\subsection{Struktura pracy}

Rozdział~\ref{sec:analiza} zawiera analizę wymagań i przegląd istniejących rozwiązań. Rozdział~\ref{sec:technologie} omawia wybrane technologie i uzasadnia decyzje projektowe. Rozdział~\ref{sec:architektura} opisuje architekturę systemu. Rozdziały~\ref{sec:backend}--\ref{sec:agent} szczegółowo omawiają implementację poszczególnych komponentów programowych. Rozdział~\ref{sec:cad} opisuje projekt mechaniczny obudowy w SolidWorks. Rozdział~\ref{sec:rpi} omawia konfigurację Raspberry Pi, automatyzację przez cron i tunel Cloudflare. Rozdział~\ref{sec:wdrozenie} dotyczy wdrożenia i infrastruktury chmurowej Azure. Rozdział~\ref{sec:testy} przedstawia wyniki testów. Praca kończy się podsumowaniem w roziale~\ref{sec:podsumowanie}.

\newpage

% ============================================================
%  ROZDZIAŁ 2 — ANALIZA WYMAGAŃ
% ============================================================

\section{Analiza wymagań i przegląd rozwiązań}
\label{sec:analiza}

\subsection{Wymagania funkcjonalne}

Na podstawie analizy potrzeb laboratorium studenckiego sformułowano następujące wymagania funkcjonalne:

\textbf{Dla użytkowników:}
\begin{itemize}
	\item rejestracja z weryfikacją adresu e-mail (ograniczoną do domeny uczelni \texttt{@uwr.edu.pl}),
	\item logowanie z użyciem tokenów JWT (access token + refresh token),
	\item przesyłanie plików G-code (maksymalnie 50~MB, format \texttt{.gcode}),
	\item planowanie druku na wybrany termin lub dodanie do kolejki ogólnej,
	\item przeglądanie własnych zadań z filtrowaniem według statusu,
	\item podgląd postępu druku w czasie rzeczywistym (pasek postępu, temperatura dyszy i podłoża, obraz z kamery),
	\item anulowanie i ponowne uruchamianie własnych zadań.
\end{itemize}

\textbf{Dla administratorów:}
\begin{itemize}
	\item pogląd i zarządzanie wszystkimi użytkownikami systemu,
	\item zmiana roli użytkownika (użytkownik/administrator),
	\item podgląd i zarządzanie wszystkimi zadaniami druku,
	\item potwierdzenie gotowości drukarki do kolejnego zlecenia,
	\item dostęp do metryk systemowych (liczba zadań według statusu).
\end{itemize}

\subsection{Wymagania niefunkcjonalne}

\begin{itemize}
	\item \textbf{Skalowalność} - architektura powinna umożliwiać dodanie kolejnych drukarek bez przebudowy systemu.
	\item \textbf{Niezawodność} - utrata wiadomości z kolejki nie powinna powodować trwałej utraty zadania.
	\item \textbf{Bezpieczeństwo} - dostęp do API chroniony tokenami JWT, hasła przechowywane w formie szyfru bcrypt.
	\item \textbf{Responsywność interfejsu} - aplikacja frontendowa powinna działać zarówno na urządzeniach desktopowych, jak i mobilnych.
	\item \textbf{Czas odpowiedzi} - operacje API powinny odpowiadać w czasie poniżej 2 sekund w warunkach normalnego obciążenia.
	\item \textbf{Przenośność} - wszystkie komponenty opakowane w kontenery Docker, uruchamiane jednym poleceniem.
\end{itemize}

\subsection{Przegląd istniejących rozwiązań}

\subsubsection{Octoprint}

OctoPrint~\cite{octoprint} jest najpopularniejszym oprogramowaniem do zarządzania drukarkami 3D. Działa jako serwer HTTP na Raspberry Pi i udostępnia interfejs webowy, API REST oraz system wtyczek. Umożliwia zdalne sterowanie drukarką, podgląd z kamery i monitorowanie postępu.

OctoPrint jest jednak przeznaczony dla jednego użytkownika zarządzającego jedną drukarką. Nie oferuje mechanizmu kolejkowania zadań wielu użytkowników, systemu uwierzytelniania opartego na rolach ani integracji z chmurą. W systemie AddiPi OctoPrint pełni rolę lokalnego sterownika drukarki, nad którym nadbudowano opisywaną w tej pracy warstwę zarządzania.

\subsubsection{Mainsail i Fluidd}

Mainsail i Fluidd to nowoczesne interfejsy webowe dla firmware Klipper. Podobnie jak OctoPrint, skierowane są do jednego użytkownika i nie przewidują obsługi kolejki wielu użytkowników.

\subsubsection{Rozwiązania komercyjne}

Oblio, Polar Cloud i Ultimaker Digital Factory to komercyjne platformy zarządzania flotą drukarek 3D w środowisku przemysłowym i edukacyjnym. Oferują zaawansowane funkcje, jednak wiążą się z kosztami subskrypcji i nie pozwalają na pełną kontrolę nad infrastrukturą - co w warunkach laboratorium akademickiego jest istotną przeszkodą.

\subsubsection{Wnioski}

Żadne z dostępnych rozwiązań o otwartym kodzie źródłowym nie łączy w sobie: systemu uwierzytelniania obsługującego wielu użytkowników, kolejkowania zadań z planowaniem czasowym, integracji z chmurą obliczeniową i sterowaniem przez IoT. System AddiPi wypełnia tę lukę, pozostając jednocześnie w pełni samodziałający i wdrażany lokalnie.

\newpage

% ============================================================
%  ROZDZIAŁ 3 — TECHNOLOGIE
% ============================================================

\section{Wybór technologii}
\label{sec:technologie}

\subsection{Języki programowania}

\subsubsection{TypeScript / Node.js}

Większość serwisów backendowych oraz aplikacja frontendowa zostały napisane w TypeScript~\cite{typescript} - nadzbiorze języka JavaScript dodającym statyczne typowanie. TypeScript znacznie ułatwia utrzymanie dużych baz kodu dzięki wykrywaniu błędów na etapie kompilacji, podpowiedziom IDE i samodokumentowaniu przez typy.

Node.js~\cite{nodejs} wybrano ze względu na asynchroniczny, nieblokujący model wejścia-wyjścia, który sprawdza się w serwisach obsługujących wiele równoległych połączeń HTTP oraz komunikację z zewnętrznymi usługami (baza danych, kolejka, IoT Hub).

\subsubsection{Python}

Agent działający na platformie Raspberry Pi został zaimplementowany w języku Python~\cite{python}. Biblioteki \texttt{azure-iot-device} oraz \texttt{azure-storage-blob} są w tym środowisku powszechnie dostępne i szczegółowo udokumentowane. Python stanowi standardowe rozwiązanie w przypadku aplikacji uruchamianych na systemach wbudowanych oraz mikrokomputerach jednoukładowych (ang. \textit{Single-Board Computer}, SBC).

\subsection{Framework webowy - Express.js}

Wszystkie serwisy platformy Node.js wykorzystują framework Express.js~\cite{express} w wersji 5.0. Rozwiązanie to charakteryzuje się minimalistyczną architekturą i niskim narzutem wydajnościowym. Framework nie narzuca sztywnej struktury katalogów (ang. \textit{unopinionated framework}), co pozwala na elastyczne dostosowanie architektury każdego z mikroserwisów do jego specyficznych wymagań. Express.js zapewnia również ustandaryzowane mechanizmy obsługi żądań pośredniczących (ang. \textit{middleware}), routingu oraz zarządzania błędami.

\subsection{Baza danych - Azure Cosmos DB}

Azure Cosmos DB~\cite{cosmosdb} to w pełni zarządzana baza NoSQL firmy Microsoft, oferująca globalną dystrybucję danych, elastyczne partycjonowanie oraz obsługę wielu modeli API (SQL, MongoDB, Cassandra, Gremlin, Table). W projekcie AddiPi wykorzystano API SQL (Core), umożliwiające wykonywanie zapytań zbliżonych składnią do języka SQL na dokumentach zapisanych w formacie JSON.

Wybór Azure Cosmos DB został podyktowany następującymi czynnikami:
\begin{itemize}
	\item brak konieczności zarządzania infrastrukturą serwera bazy danych dzięki wykorzystaniu modelu PaaS (Platform as a Service),
	\item elastyczny schemat dokumentów umożliwiający przechowywanie danych o różnej strukturze w ramach jednej bazy danych (\texttt{addipi}), z podziałem na odrębne kontenery (\texttt{users}, \texttt{jobs}, \texttt{refresh-tokens}),
	\item natywna integracja z ekosystemem Microsoft Azure,
	\item automatyczne indeksowanie wszystkich pól dokumentów.
\end{itemize}

\subsection{Przechowywanie plików - Azure Blob Storage}

Pliki G-code przesyłane przez użytkowników są przechowywane w usłudze Azure Blob Storage~\cite{blobstorage}, w kontenerze \texttt{gcode}. Usługa ta zapewnia praktycznie nieograniczoną przestrzeń magazynową, relatywnie niskie koszty przechowywania danych oraz prosty interfejs umożliwiający przesyłanie i pobieranie obiektów binarnych.

\subsection{Kolejkowanie - Azure Service Bus}

Azure Service Bus~\cite{servicebus} to chmurowa usługa kolejkowania komunikatów klasy enterprise firmy Microsoft. W systemie AddiPi pełni rolę brokera komunikatów pomiędzy usługami Files Service (producent komunikatów) oraz Queue Service (konsument).

Kolejka \texttt{print-queue} zapewnia semantykę dostarczania wiadomości typu \newline \texttt{at-least-once}. Oznacza to, że w przypadku awarii usługi konsumenckiej komunikat pozostaje w kolejce i może zostać przetworzony powtórnie po ponownym uruchomieniu konsumenta.

\subsection{Sterowanie urządzeniem - Azure IoT Hub}

Azure IoT Hub~\cite{iothub} to usługa firmy Microsoft przeznaczona do zarządzania urządzeniami Internetu Rzeczy (IoT) w środowisku chmurowym. Umożliwia dwukierunkową komunikację z urządzeniami (Device-to-Cloud oraz Cloud-to-Device), a także wywołanie metod bezpośrednich (Direct Methods), stanowiących synchroniczne wywołania RPC (zdalne wywołanie procedury) wykonane na urządzeniach komunikujących się za pośrednictwem protokołów MQTT lub AMQP.

W projekcie AddiPi usługa Printer Service wywołuje na agencie działającym na urządzeniu Raspberry Pi metody \texttt{startPrint}, \texttt{cancelPrint} oraz \texttt{getStatus}. Agent przesyła następnie telemetrię w postaci zdarzeń, takich jak \texttt{print\_started}, \texttt{print\_progress} czy \texttt{print\_completed}, które są odbierane przez usługę Printer Service za pośrednictwem endpointu kompatybilnego z Azure Event Hubs.


\subsection{Frontend - React i Vite}

Interfejs użytkownika został zbudowany z wykorzystaniem biblioteki React~\cite{react} 18 oraz języka TypeScript. Jako serwer deweloperski i narzędzie do budowania aplikacji wykorzystano Vite~\cite{vite}.

Do zarządzania stanem globalnym aplikacji zastosowano bibliotekę Zustand~\cite{zustand}, umożliwiającą współdzielenie danych pomiędzy komponentami aplikacji w uproszczony sposób, bez konieczności implementowania rozbudowanej logiki zarządzania stanem. 

Warstwa stylów została zrealizowana przy użyciu frameworka Tailwind CSS~\cite{tailwind}, który opiera się na wykorzystaniu niewielkich klas CSS reprezentujących pojedyncze właściwości stylów interfejsu użytkownika.

\subsection{Kontenery - Docker i Docker Compose}

Każdy serwis aplikacji posiada własny plik \texttt{Dockerfile} realizujący wieloetapowy proces budowania obrazu kontenera (\textit{multi-stage build}). Rozwiązanie to pozwala na ograniczenie rozmiaru finalnych obrazów oraz oddzielenie środowiska kompilacji od uruchomieniowego.

Docker Compose~\cite{docker} wykorzystano do zarządzania uruchamianiem wszystkich serwisów zarówno w środowisku lokalnym, jak i produkcyjnym. Narzędzie definiuje wspólną sieć \texttt{addipi-network} oraz konfigurację zmiennych środowiskowych odczytywanych z pliku \texttt{.env}.

\subsection{CAD - SolidWorks}

Projekt mechaniczny obudowy urządzenia wykonano w programie \textbf{SolidWorks}~\cite{solidworks}, stanowiącym środowisko do parametrycznego modelowania bryłowego oraz projektowania złożeń mechanicznych. Oprogramowanie umożliwia tworzenie asocjatywnych modeli 3D, w których modyfikacja parametrów części powoduje automatyczną aktualizację złożeń oraz dokumentacji technicznej.

Wybór programu SolidWorks był podyktowany dostępnością oprogramowania w ramach licencji studenckiej oraz szerokim wsparciem dla formatów eksportu modeli, takich jak \texttt{.STEP} (neutralny format wymiany modeli CAD), \texttt{.STL} (format wykorzystywany w procesie przygotowania modeli o druku 3D) oraz \texttt{.3MF} (format wymiany zawierający dodatkowe metadane procesu druku).

Projekt został podzielony na pliki\texttt{.SLDPRT}, reprezentujące pojedyncze części, oraz \texttt{.SLDASM}, definiujące kompletne złożenia mechaniczne.

\subsection{Porównanie wybranych alternatyw}

\begin{table}[h!]
	\centering
	\caption{Porównanie baz danych rozważanych w projekcie}
	\begin{tabular}{lccc}
		\toprule
		\textbf{Cecha} & \textbf{Cosmos DB} & \textbf{PostgreSQL} & \textbf{MongoDB Atlas} \\
		\midrule
		Model danych & Dokument (JSON) & Relacyjny & Dokument (BSON) \\
		Zarządzanie & PaaS (bez serwera) & Samodzielny/PaaS & PaaS \\
		Integracja z Azure & Natywna & Ograniczona & Pośrednia \\
		\makecell[l]{Elastyczny \\ schemat danych} & Tak & Nie & Tak \\
		\makecell[l]{Dostępność \\ bezpłatnego planu} & Tak (400 RU/s) & Tak (self-hosted) & Tak (512 MB) \\
		\bottomrule
	\end{tabular}
\end{table}

\begin{table}[h!]
	\centering
	\caption{Porównanie rozwiązań kolejkowania wiadomości}
	\begin{tabular}{lccc}
		\toprule
		\textbf{Cecha} & \textbf{Azure Service Bus} & \textbf{RabbitMQ} & \textbf{Apache Kafka} \\
		\midrule
		Model wdrożenia & PaaS & Self-hosted & Self-hosted / PaaS \\
		Semantyka dostarczania & At-least-once & At-least-once & At-least-once \\
		Złożoność konfiguracji & Niska & Średnia & Wysoka \\
		Integracja z Azure & Natywna & Ograniczona & Pośrednia \\
		Skalowalność & Automatyczna & Ręczna & Wysoka \\
		\bottomrule
	\end{tabular}
\end{table}

\newpage

% ============================================================
%  ROZDZIAŁ 4 — ARCHITEKTURA SYSTEMU
% ============================================================
\section{Architektura systemu}
\label{sec:architektura}

\subsection{Ogólna struktura}

System AddiPi zbudowany jest w architekturze mikroserwisowej. Każdy serwis stanowi niezależną jednostkę wdrożeniową odpowiedzialną za określoną domenę biznesową oraz komunikuje się z pozostałymi komponentami za pośrednictwem zdefiniowanych interfejsów (HTTP REST lub kolejka komunikatów).

W ramach systemu wyodrębniono następujące komponenty:

\begin{enumerate}
	\item \textbf{AddiPi Auth Service} (port 3001) - realizuje proces uwierzytelniania użytkowników oraz obsługę tokenów JWT.
	
	\item \textbf{AddiPi User Service} (port 3002) - odpowiada za zarządzanie profilami użytkowników oraz historią zadań do wydruku.
	
	\item \textbf{AddiPi Files Service} (port 5000) - umożliwia przesyłanie oraz przechowywanie plików G-code.
	
	\item \textbf{AddiPi Queue Service} (porty 3070) - odbiera komunikaty z kolejki oraz zapisuje informacje o zadaniach w bazie Azure Cosmos DB.
	
	\item \textbf{AddiPi Printer Service} (port 3050) - odpowiada za harmonogramowanie zadań do wydruku, komunikację z Azure IoT Hub oraz odbiór telemetrii urządzenia.
	
	\item \textbf{AddiPi Video Service} (port 3003) - odpowiada za rejestrację i udostępnianie adresu URL kamery systemu OctoPrint.
	
	\item \textbf{AddiPi Agent} (Raspberry Pi) - agent uruchamiany na urządzeniu Raspberry Pi, pośredniczący pomiędzy Azure IoT Hub a systemem OctoPrint.
	
	\item \textbf{AddiPi Frontend} (Vercel / port 5173) - aplikacja webowa zbudowana z wykorzystaniem biblioteki React.
\end{enumerate}

\subsection{Diagram architektury}

Na rysunku~\ref{fig:architektura} przedstawiono sekwencyjny przepływ danych pomiędzy poszczególnymi komponentami zaprojektowanego systemu.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=1\textwidth]{diagram-architektury.pdf} 
	\caption{Diagram architektury systemu.}
	\label{fig:architektura}
\end{figure}



\begin{enumerate}
	\item \textbf{Uwierzytelnianie użytkownika:} Użytkownik loguje się w systemie za pośrednictwem aplikacji klienckiej (frontendowej). Aplikacja nawiązuje komunikację z usługą \textit{Auth Service}, która po pomyślnej weryfikacji tożsamości zwraca parę tokenów JWT (\textit{JSON Web Token}).
	
	\item \textbf{Przesyłanie pliku produkcyjnego:} Użytkownik przesyła plik z kodem sterującym (G-code). Aplikacja frontendowa wywołuje punkt końcowy (ang. \textit{endpoint}) \texttt{POST /files/upload} w ramach usługi \textit{Files Service}. Następnie komponent ten zapisuje plik w repozytorium \textit{Azure Blob Storage} oraz publikuje komunikat \texttt{file\_uploaded} w kolejce usługi \textit{Azure Service Bus}.
	
	\item \textbf{Inicjalizacja zadania w bazie danych:} Usługa \textit{Queue Service} asynchronicznie odbiera komunikaty z kolejki i na ich podstawie tworzy dokument zadania w bazie danych \textit{Azure Cosmos DB} (w ramach kontenera \texttt{jobs}), przypisując mu status początkowy \texttt{pending} lub \texttt{scheduled}.
	
	\item \textbf{Harmonogramowanie i weryfikacja stanu urządzenia:} Usługa \textit{Printer Service} w sposób cykliczny (z interwałem jednominutowym) weryfikuje dostępność zadań oczekujących na realizację. Jeżeli urządzenie drukujące jest wolne, a zadanie spełnia kryteria rozpoczęcia procesu wytwórczego, status transakcji jest zmieniany na \texttt{waiting\_for\_printer\_ready}. System przechodzi wówczas w stan oczekiwania na potwierdzenie gotowości operacyjnej drukarki.
	
	\item \textbf{Inicjacja komunikacji IoT:} Po autoryzacji gotowości urządzenia przez użytkownika lub administratora, usługa \textit{Printer Service} zdalnie wywołuje metodę bezpośrednią (ang. \textit{Direct Method}) \texttt{startPrint} na dedykowanym agencie, wykorzystując do tego celu usługę \textit{Azure IoT Hub}.
	
	\item \textbf{Zarządzanie procesem drukowania przez agenta:} Komponent \textit{AddiPi Agent} pobiera przypisany plik G-code z repozytorium \textit{Azure Blob Storage}, przekazuje go bezpośrednio do interfejsu oprogramowania \textit{OctoPrint} oraz inicjuje proces drukowania 3D. W trakcie pracy agent w sposób ciągły transmituje dane telemetryczne do usługi \textit{Azure IoT Hub}, raportując bieżący postęp, status ukończenia lub ewentualne błędy krytyczne.
	
	\item \textbf{Odbiór telemetrii i aktualizacja stanu:} Usługa \textit{Printer Service} konsumuje strumień danych telemetrycznych za pośrednictwem punktu końcowego zgodnego ze specyfikacją \textit{Azure Event Hubs}, a następnie aktualizuje stan procesu w bazie \textit{Azure Cosmos DB}.
	
	\item \textbf{Odświeżanie danych w interfejsie:} Aplikacja kliencka (frontendowa) realizuje cykliczne zapytania sondujące (ang. \textit{polling} z interwałem 5 sekund) do mikroserwisów, zapewniając bieżącą aktualizację stanu systemu prezentowanego użytkownikowi.
\end{enumerate}

\subsection{Model danych}

W bazie danych \textit{Azure Cosmos DB} dane są strukturyzowane i przechowywane w postaci dokumentów w formacie JSON. Zostały one pogrupowane w logicznych kontenerach, odpowiadających poszczególnym domenom biznesowym systemu.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=1\textwidth]{diagram-architektury.pdf} 
	\caption{Diagram architektury systemu.}
	\label{fig:db_schema}
\end{figure}


\subsubsection{Użytkownik (kontener \texttt{users})}

Dokument przechowuje informacje profilowe użytkowników, dane autoryzacyjne oraz status weryfikacji konta. Przykładową strukturę przedstawiono na listingu~\ref{lst:json_user}.

\begin{lstlisting}[language=,caption={Przykładowy dokument użytkownika w Azure Cosmos DB},style=jsonStyle, label={lst:json_user}]
	{
		"id": "user_<timestamp>",
		"email": "student@uwr.edu.pl",
		"firstName": "Jan",
		"lastName": "Kowalski",
		"password": "<bcrypt hash>",
		"role": "user",
		"isVerified": true,
		"verificationToken": "<hex>",
		"verificationTokenExpiry": "2025-01-01T00:00:00Z",
		"createdAt": "2025-01-01T00:00:00Z",
		"updatedAt": "2025-01-01T00:00:00Z"
	}
\end{lstlisting}

Pole \texttt{role} definiuje uprawnienia w systemie i może przyjmować wartość \texttt{user} (użytkownik standardowy) lub \texttt{admin} (administrator systemu).

\subsubsection{Zadanie do wydruku (kontener \texttt{jobs})}

Dokument zadania (listing~\ref{lst:json_job}) agreguje informacje o pliku sterującym, powiązanym użytkowniku, metrykach czasu oraz bieżącym stanie realizacji procesu produkcyjnego.

\begin{lstlisting}[language=,caption={Przykładowy dokument zadania do wydruku w Cosmos DB},style=jsonStyle, label={lst:json_job}]
	{
		"id": "<timestamp string>",
		"fileId": "20251203_014648_model.gcode",
		"originalFileName": "model.gcode",
		"userId": "user_<timestamp>",
		"userEmail": "student@uwr.edu.pl",
		"status": "printing",
		"scheduledAt": "2025-12-01T15:00:00Z",
		"createdAt": "2025-11-30T10:00:00Z",
		"startedAt": "2025-12-01T15:01:00Z",
		"completedAt": "2025-12-01T17:30:00Z",
		"progress": 87.5,
		"printTime": 4500,
		"printTimeLeft": 600,
		"deviceId": "raspberry-pi-mkt-01",
		"failureReason": null
	}
\end{lstlisting}

Pole \texttt{status} może przyjmować wartości:
\texttt{pending}, \texttt{scheduled}, \newline
\texttt{waiting\_for\_printer\_ready},
\texttt{delayed}, \texttt{printing},
\texttt{completed}, \texttt{failed},
\texttt{cancelled}.

\subsubsection{Token odświeżania (kontener \texttt{refresh-tokens})}

W celu zapewnienia bezpiecznej, bezstanowej autoryzacji sesji, tokeny odświeżania (ang. \textit{refresh tokens}) są utrwalane w dedykowanym kontenerze (listing~\ref{lst:json_token}).

\begin{lstlisting}[language=,caption={Przykładowy dokument refresh tokena},style=jsonStyle, label={lst:json_token}]
	{
		"id": "<userId>_<timestamp>",
		"userId": "user_<timestamp>",
		"token": "<JWT string>",
		"expiresAt": "2025-12-08T00:00:00Z",
		"createdAt": "2025-12-01T00:00:00Z"
	}
\end{lstlisting}

\subsection{Cykl życia zadania do wydruku}

Zadanie w procesie przetwarzania i realizacji może znajdować się w jednym z następujących stanów:

\begin{itemize}
	\item \textbf{pending} --- zadanie bez określonego terminu realizacji, oczekujące w kolejce ogólnej systemu.
	
	\item \textbf{scheduled} --- zadanie posiadające ściśle zaplanowany termin realizacji, oczekujące na osiągnięcie wskazanego czasu rozpoczęcia. Po nadejściu zaplanowanego czasu zadanie to otrzymuje najwyższy priorytet wykonania.
	
	\item \textbf{waiting\_for\_printer\_ready} --- usługa harmonogramująca (ang. \textit{scheduler}) wybrała zadanie do realizacji; system oczekuje na manualne potwierdzenie gotowości urządzenia przez użytkownika.
	
	\item \textbf{delayed} --- stan opóźnienia, w którym system oczekuje na potwierdzenie gotowości drukarki dłużej niż 3~h; do użytkownika wysyłane jest automatyczne powiadomienie przypominające.
	
	\item \textbf{printing} --- proces drukowania modelu sterującego jest aktywnie realizowany przez oprogramowanie \textit{OctoPrint}.
	
	\item \textbf{completed} --- proces drukowania 3D został pomyślnie sfinalizowany.
	
	\item \textbf{failed} --- w trakcie realizacji procesu produkcyjnego wystąpił błąd krytyczny uniemożliwiający dalsze drukowanie.
	
	\item \textbf{cancelled} --- zadanie zostało anulowane ręcznie przez użytkownika bądź administratora lub automatycznie przez system po upływie 48~h oczekiwania na potwierdzenie gotowości urządzenia.
\end{itemize}

Na rysunku~\ref{fig:state_machine} przedstawiono formalny model maszyny stanów, obrazujący dopuszczalne przejścia fazowe w cyklu życia zadania produkcyjnego.

\begin{figure}[H]
	\centering
	\includegraphics[width=1\textwidth]{cykl-zycia-zadania.pdf} 
	\caption{Diagram maszyny stanów cyklu życia zadania do wydruku.}
	\label{fig:state_machine}
\end{figure}

\subsection{Bezpieczeństwo i uwierzytelnianie}

W celu zapewnienia poufności danych oraz kontroli dostępu, w systemie zaimplementowano dwutokenowy model uwierzytelniania oparty na standardzie JWT (\textit{JSON Web Token}). Mechanizm ten wykorzystuje dwa rodzaje obiektów:

\begin{itemize}
	\item \textbf{Access token} --- token dostępu o krótkim czasie autoryzacji (ważny przez 15 minut), przekazywany w nagłówku protokołu HTTP (\texttt{Authorization: Bearer <token>}). Integralność i autentyczność tokenu są weryfikowane kryptograficznie przy użyciu klucza symetrycznego \texttt{JWT\_SECRET}.
	
	\item \textbf{Refresh token} --- token odświeżający o wydłużonym czasie ważności (wynoszącym 7 dni), utrwalany w bazie danych \textit{Azure Cosmos DB} oraz przechowywany po stronie aplikacji klienckiej. Podpisywany jest przy użyciu dedykowanego klucza \texttt{JWT\_REFRESH\_SECRET}. Służy do generowania nowej pary tokenów bez konieczności ponownego wprowadzania danych uwierzytelniających przez użytkownika.
\end{itemize}

Hasła użytkowników nie są przechowywane w bazie danych w postaci jawnej. Do ich zabezpieczenia wykorzystano algorytm funkcjonału skrótu \textit{bcrypt} z parametrem kosztu (ang. \textit{salt rounds}) ustawionym na wartość 10.

Proces rejestracji nowych kont został ze względów bezpieczeństwa ograniczony wyłącznie do adresów poczty elektronicznej w domenie akademickiej \texttt{@uwr.edu.pl}. Pełna aktywacja profilu wymaga weryfikacji dwuetapowej poprzez kliknięcie w unikalny link aktywacyjny przesłany na adres e-mail użytkownika.

Kontrola dostępu na poziomie kodu realizowana jest za pomocą oprogramowania pośredniczącego (ang. \textit{middleware}):
\begin{itemize}
	\item \texttt{requireAuth} --- odpowiada za ekstrakcję i weryfikację tokenu dostępu poprzez wysłanie wewnętrznego żądania weryfikacyjnego do punktu końcowego \texttt{POST \newline /auth/verify} usługi \textit{Auth Service}.
	\item \texttt{requireAdmin} --- realizuje mechanizm kontroli dostępu opartej na rolach (ang. \textit{Role-Based Access Control}, RBAC), sprawdzając, czy zdekodowany profil użytkownika posiada przypisane uprawnienia klasy \texttt{admin}.
\end{itemize}

% ============================================================
%  ROZDZIAŁ 5 — IMPLEMENTACJA BACKENDU
% ============================================================
\section{Implementacja serwisów backendowych}
\label{sec:backend}

\subsection{Auth Service}

\subsubsection{Opis ogólny}

Usługa \textit{Auth Service} odpowiada za procesy uwierzytelniania, autoryzacji oraz zarządzania kontami użytkowników. W zakres jej funkcjonalności wchodzi: rejestracja, logowanie i wylogowanie użytkowników, odświeżanie tokenów sesyjnych oraz weryfikacja adresów poczty elektronicznej.

Komponent ten został zaimplementowany w środowisku uruchomieniowym Node.js z wykorzystaniem frameworka Express.js oraz języka TypeScript. Serwis realizuje operacje wejścia-wyjścia poprzez komunikację z bazą danych \textit{Azure Cosmos DB} (w ramach kontenerów \texttt{users} oraz \texttt{refresh-tokens}). Ponadto integruje się z zewnętrznym serwerem SMTP firmy Google w celu dystrybucji wiadomości aktywacyjnych, wykorzystując do tego celu bibliotekę \texttt{Nodemailer}.


\subsubsection{Punkty końcowe API}

W tabeli~\ref{tab:auth_endpoints} zestawiono i opisano punkty końcowe (ang. \textit{endpoints}) udostępniane przez usługę \textit{Auth Service}.

\begin{table}[htbp]
	\centering
	\caption{Punkty końcowe usługi Auth Service.}
	\label{tab:auth_endpoints}
	\begin{tabular}{llp{7cm}}
		\toprule
		\textbf{Metoda} & \textbf{Ścieżka} & \textbf{Opis} \\
		\midrule
		POST & \texttt{/auth/register} & Rejestracja nowego użytkownika, walidacja domeny e-mail, generowanie szyfru hasła oraz wysłanie wiadomości z linkiem aktywacyjnym. \\
		\addlinespace
		POST & \texttt{/auth/login} & Uwierzytelnienie użytkownika; generowanie i zwrot pary tokenów: \textit{access token} oraz \textit{refresh token}. \\
		\addlinespace
		POST & \texttt{/auth/logout} & Unieważnienie tokenu odświeżającego i zakończenie aktywnej sesji użytkownika. \\
		\addlinespace
		PATCH & \texttt{/auth/refresh} & Generowanie nowego tokenu dostępu na podstawie ważnego tokenu odświeżającego. \\
		\addlinespace
		POST & \texttt{/auth/verify} & Weryfikacja poprawności tokenu dostępu, wykorzystywana wewnętrznie przez pozostałe mikroserwisy. \\
		\addlinespace
		GET & \texttt{/auth/verify-email} & Aktywacja konta użytkownika po odebraniu i weryfikacji tokenu z linku aktywacyjnego. \\
		\addlinespace
		POST & \texttt{/auth/resend-verification} & Ponowne wygenerowanie i wysłanie wiadomości weryfikacyjnej na adres e-mail. \\
		\addlinespace
		GET & \texttt{/health} & Punkt końcowy diagnostyki stanu działania usługi (ang. \textit{health check}). \\
		\bottomrule
	\end{tabular}
\end{table}

\subsubsection{Kluczowe fragmenty implementacji}

Na listingu~\ref{lst:jwt_generation} przedstawiono implementację funkcji odpowiedzialnych za generowanie tokenu dostępu oraz tokenu odświeżającego przy użyciu biblioteki \texttt{jsonwebtoken}.

\begin{lstlisting}[language=TypeScript,caption={Implementacja generowania tokenów JWT (\texttt{token-service.ts})},style=tsStyle,label={lst:jwt_generation}]
export function generateAccessToken(user: User): string {
	const payload: JWTPayload = {
		userId: user.id,
		email: user.email,
		role: user.role
	};
	return jwt.sign(payload, CONFIG.JWT_SECRET, { expiresIn: '15m' });
}

export function generateRefreshToken(user: User): string {
	return jwt.sign(
	{ userId: user.id },
	CONFIG.JWT_REFRESH_SECRET,
	{ expiresIn: '7d' }
	);
}
\end{lstlisting}

Proces walidacji tokenu odświeżającego (ang. \textit{refresh token}) jest dwuetapowy. Obejmuje zarówno kryptograficzną weryfikację poprawności podpisu cyfrowego struktury JWT, jak i asynchroniczne sprawdzenie obecności oraz ważności powiązanego rekordu w bazie danych \textit{Azure Cosmos DB}. Takie podejście architektoniczne umożliwia natychmiastowe, zdalne unieważnianie sesji (ang. \textit{token revocation}) w momencie wylogowania się użytkownika z systemu. Kod realizujący tę procedurę zaprezentowano na listingu~\ref{lst:jwt_validation}.

\begin{lstlisting}[language=TypeScript,caption={Implementacja walidacji refresh tokenu (\texttt{token-service.ts})},style=tsStyle,label={lst:jwt_validation}]
export async function validateRefreshToken(
token: string
): Promise<string | null> {
	try {
		const decoded = jwt.verify(
		token,
		CONFIG.JWT_REFRESH_SECRET
		) as { userId: string };
		
		const query = `SELECT * FROM c
		WHERE c.token = @token
		AND c.userId = @userId
		AND c.expiresAt > @now`;
		
		const { resources } = await refreshTokensContainer.items.query({
			query,
			parameters: [
			{ name: '@token', value: token },
			{ name: '@userId', value: decoded.userId },
			{ name: '@now', value: new Date().toISOString() }
			]
		}).fetchAll();
		
		return resources.length > 0 ? decoded.userId : null;
	} catch {
		return null;
	}
}
\end{lstlisting}

\subsubsection{Weryfikacja adresu poczty elektronicznej}

Po pomyślnym zakończeniu procesu rejestracji system generuje unikalny, 64-znakowy token w formacie szesnastkowym. Jest on tworzony przy użyciu kryptograficznie bezpiecznego generatora liczb pseudolosowych z modułu \texttt{crypto} (\texttt{crypto.randomBytes}). Zapewnia to wysoką entropię i odporność na ataki typu \textit{brute-force}.

Wygenerowany identyfikator jest zapisywany w bazie danych wraz z datą ważności, a do użytkownika wysyłana jest wiadomość e-mail zawierająca odsyłacz aktywacyjny skierowany do punktu końcowego systemu:
\begin{center}
	\texttt{GET /auth/verify-email?token=<hex>}
\end{center}

Po odebraniu żądania przez usługę \textit{Auth Service}, system przeprowadza weryfikację poprawności oraz ważności tokenu. W przypadku pozytywnego rezultatu walidacji, atrybut \texttt{isVerified} w dokumencie użytkownika przyjmuje wartość \texttt{true}, a konto zostaje w pełni aktywowane. Na zakończenie procesu serwer wykonuje przekierowanie (ang. \textit{HTTP redirect}) przeglądarki użytkownika do adresu bazowego aplikacji klienckiej.

\subsection{User Service}

\subsubsection{Opis ogólny}

Usługa \textit{User Service} realizuje zadania związane z zarządzaniem profilami użytkowników, modyfikacją ich uprawnień oraz agregacją danych dotyczących powiązanych zadań wytwórczych. Komponent ten wykonuje operacje odczytu i zapisu danych w ramach kontenerów \texttt{users} oraz \texttt{jobs} w bazie danych \textit{Azure Cosmos DB}.

Autoryzacja żądań oraz weryfikacja tożsamości użytkowników są realizowane w sposób scentralizowany poprzez przesyłanie wewnętrznych zapytań do punktu końcowego \texttt{POST /auth/verify} opisanej wcześniej usługi \textit{Auth Service}.

\subsubsection{Punkty końcowe API}

W tabeli~\ref{tab:user_endpoints} wyszczególniono i opisano punkty końcowe (ang. \textit{endpoints}) udostępniane przez usługę \textit{User Service}, z uwzględnieniem podziału na operacje standardowe oraz administracyjne.

\begin{xltabular}{\textwidth}{llX}
	\caption{Punkty końcowe usługi User Service.} \label{tab:user_endpoints} \\
	\toprule
	\textbf{Metoda} & \textbf{Ścieżka} & \textbf{Opis} \\
	\midrule
	\endfirsthead
	
	% Nagłówek powtarzany na kolejnej stronie
	\multicolumn{3}{c}{{\bfseries \tablename\ \thetable{} -- ciąg dalszy z poprzedniej strony}} \\
	\toprule
	\textbf{Metoda} & \textbf{Ścieżka} & \textbf{Opis} \\
	\midrule
	\endhead
	
	% Linia dolna na stronach, gdzie tabela będzie się dzielić
	\bottomrule
	\endfoot
	
	% Linia dolna na samym końcu tabeli
	\bottomrule
	\endlastfoot
	
	GET & \texttt{/users/me} & Pobranie danych profilowych zalogowanego użytkownika. \\
	\addlinespace
	PATCH & \texttt{/users/me} & Aktualizacja danych osobowych użytkownika (imię oraz nazwisko). \\
	\addlinespace
	GET & \texttt{/users/me/jobs} & Pobranie historii wszystkich zadań przypisanych do zalogowanego użytkownika. \\
	\addlinespace
	GET & \texttt{/users/me/stats} & Pobranie statystyk użytkownika, obejmujących m.in. zagregowaną liczbę zadań z podziałem na ich statusy. \\
	\addlinespace
	GET & \texttt{/users/jobs/upcoming} & Pobranie listy zadań oczekujących na realizację (posiadających status \texttt{pending} lub \texttt{scheduled}). \\
	\addlinespace
	GET & \texttt{/users/jobs/recent-completed} & Pobranie historycznej listy ostatnio sfinalizowanych procesów druku. \\
	\addlinespace
	GET & \texttt{/users} & Pobranie rejestru wszystkich użytkowników systemu (dostęp zastrzeżony dla administratora). \\
	\addlinespace
	PATCH & \texttt{/users/:id/role} & Modyfikacja roli i poziomu uprawnień użytkownika o wskazanym identyfikatorze (dostęp zastrzeżony dla administratora). \\
	\addlinespace
	DELETE & \texttt{/users/:id} & Trwałe usunięcie konta użytkownika na podstawie parametru identyfikatora (dostęp zastrzeżony dla administratora). \\
	\addlinespace
	GET & \texttt{/users/:id/jobs} & Pobranie pełnej listy zadań powiązanych z wybranym kontem użytkownika (dostęp zastrzeżony dla administratora). \\
\end{xltabular}

\subsubsection{Mechanizm autoryzacji żądań}

Na listingu~\ref{lst:middleware_auth_real} zaprezentowano produkcyjną implementację oprogramowania pośredniczącego \texttt{requireAuth}. Komponent ten w pełni wykorzystuje silne typowanie języka TypeScript, w tym parametry generyczne interfejsów \texttt{Request} oraz \texttt{Response} z biblioteki \texttt{express}. 

Procedura autoryzacji polega na bezpiecznej ekstrakcji tokenu dostępu z nagłówka \texttt{Authorization}, a następnie wykonaniu asynchronicznego żądania sieciowego metodą \texttt{POST} do punktu końcowego usługi \textit{Auth Service}. Po odebraniu odpowiedzi struktura danych jest rzutowana na dedykowany typ \texttt{Data}. W przypadku poprawnej walidacji następuje asercja typu obiektu żądania, wstrzyknięcie profilu użytkownika do pola \texttt{user} oraz wywołanie funkcji \texttt{next()}, która przekazuje sterowanie do kolejnego elementu w potoku przetwarzania (ang. \textit{middleware chain}).

\begin{lstlisting}[language=TypeScript,caption={Implementacja oprogramowania pośredniczącego \texttt{requireAuth} (\texttt{requireAuth.ts})},style=tsStyle,label={lst:middleware_auth_real}]
import { CONFIG } from "../config/config";
import type { Request, Response, NextFunction } from "express";
import { Data, AuthUser } from "./mwTypes";

const requireAuth = async (
req: Request<{}, unknown, {}, {}>,
res: Response<{error: string}>,
next: NextFunction
): Promise<void | {error: string}> => {
	try {
		const token: string | undefined = req.headers.authorization?.replace('Bearer ', '');
		
		if (!token) {
			res.status(401).json({ error: 'Missing authentication token' });
			return;
		}
		
		const response: globalThis.Response = await fetch(`${CONFIG.AUTH_SERVICE_URL}/auth/verify`, {
			method: 'POST',
			headers: {
				'Authorization': `Bearer ${token}`,
				'Content-Type': 'application/json'
			}
		});
		
		const data: Data = await response.json() as Data;
		
		if (!response.ok) {
			throw new Error('Authentication failed');
		}
		
		(req as any).user = data.user;
		
		next();
	} catch (err) {
		res.status(401).json({ error: 'Invalid authentication token' });
	}
};

export default requireAuth;
\end{lstlisting}

\subsection{Files Service}

\subsubsection{Opis ogólny}

Usługa \textit{Files Service} stanowi mikroserwis zaimplementowany w języku Python z wykorzystaniem mikroframeworka Flask. Komponent ten odpowiada za asynchroniczne przyjmowanie plików z kodem sterującym (G-code) od aplikacji klienckiej, ich bezpieczne utrwalanie w repozytorium obiektowym \textit{Azure Blob Storage} oraz rozgłaszanie zdarzeń systemowych za pośrednictwem magistrali komunikatów \textit{Azure Service Bus}.

\subsubsection{Procedura przetwarzania i przesyłania plików}

Przebieg procesu przesyłania pliku oraz jego rejestracji w systemie realizowany jest sekwencyjnie według następującego scenariusza:

\begin{enumerate}
	\item \textbf{Inicjacja żądania:} Aplikacja kliencka wysyła żądanie \texttt{POST /files/upload} zawierające plik zakodowany w formacie \texttt{multipart/form-data}.
	\item \textbf{Weryfikacja uprawnień:} Oprogramowanie pośredniczące \texttt{require\_auth} deleguje proces walidacji tokenu uwierzytelniającego do usługi \textit{Auth Service}.
	\item \textbf{Walidacja potoku wejściowego:} System weryfikuje poprawność formatu pliku (akceptowane jest wyłącznie rozszerzenie \texttt{.gcode}), jego rozmiar (wprowadzono limit do 50~MB) oraz opcjonalnie strukturę wewnętrzną (w przypadku aktywacji flagi konfiguracyjnej \texttt{STRICT\_CONTENT\_CHECK}).
	\item \textbf{Sanitaryzacja i unikalność nazw:} Nazwa pliku wejściowego jest modyfikowana poprzez dodanie prefiksu czasowego zawierającego kompletną datę i godzinę odebrania żądania (np. \texttt{20251203\_014648\_model.gcode}), co zapobiega konfliktom nadpisywania zasobów.
	\item \textbf{Utrwalenie zasobu:} Plik w formacie binarnym zostaje przesłany i zapisany w dedykowanym kontenerze \texttt{gcode} w ramach usługi \textit{Azure Blob Storage}.
	\item \textbf{Publikacja zdarzenia:} Do kolejki \texttt{print-queue} magistrali \textit{Azure Service Bus} wysyłany jest komunikat w formacie JSON, który inicjuje dalsze etapy cyklu życia zadania.
\end{enumerate}

Na rysunku~\ref{fig:upload_flow} zobrazowano schematyczny przepływ danych podczas procedury przesyłania i kolejkowania pliku G-code.

\begin{figure}[H]
	\centering
	\includegraphics[width=1\textwidth]{przeplyw-przesylania-pliku.pdf} 
	\caption{Przepływ przesyłania pliku G-code --- od zapytania HTTP do integracji z kolejką.}
	\label{fig:upload_flow}
\end{figure}

Na listingu~\ref{lst:flask_upload} zaprezentowano implementację końcowego etapu obsługi żądania w ramach kontrolera usługi \textit{Files Service}. Po pomyślnej walidacji i zapisie pliku w repozytorium obiektowym, struktura danych reprezentująca zdarzenie jest serializowana do formatu JSON i przekazywana do metody nadawczej klienta \textit{Azure Service Bus}. Całość operacji została otoczona blokiem obsługi wyjątków (\texttt{try-except}), co gwarantuje stabilność działania mikroserwisu i zwrócenie ustrukturyzowanej odpowiedzi o błędzie w formacie JSON wraz ze statusem HTTP 500 w przypadku awarii infrastruktury chmuwej.

\begin{lstlisting}[language=Python,caption={Implementacja publikacji komunikatu w Azure Service Bus (\texttt{files\_controller.py})},style=pyStyle,label={lst:flask_upload}]
	message = {
		'event': 'file_uploaded',
		'fileId': filename,
		'originalFileName': original_filename,
		'userId': user_id,
		'userEmail': user_email,
		'timestamp': str(time.time()),
		'scheduledAt': request.form.get('scheduledAt')
	}
	
	sb_client = current_app.config.get('SERVICE_BUS_CLIENT')
	if sb_client:
		with sb_client:
			sender = sb_client.get_queue_sender(queue_name='print-queue')
			sender.send_messages([{'body': json.dumps(message)}])
	
	return jsonify({'status': 'success', 'fileId': filename}), 200
	
except Exception as e:
	return jsonify({'error': str(e)}), 500
\end{lstlisting}



\subsection{Queue Service}

\subsubsection{Opis ogólny}

Usługa \textit{Queue Service} to mikroserwis zaimplementowany w środowisku uruchomieniowym Node.js przy użyciu języka TypeScript oraz standardu modułów ECMAScript (ang. \textit{ECMAScript Modules}, ESM). Głównym zadaniem komponentu jest asynchroniczne i bezprzerwowe nasłuchiwanie (ang. \textit{listening}) komunikatów publikowanych w magistrali \textit{Azure Service Bus}, a następnie ich transformacja i utrwalanie w postaci dokumentów zadań produkcyjnych w bazie danych \textit{Azure Cosmos DB}.

Dodatkowo serwis udostępnia dedykowany interfejs programistyczny HTTP API, który umożliwia innym komponentom systemu (w tym aplikacji klienckiej) monitorowanie stanu kolejki oraz zarządzanie priorytetami zadań.

\subsubsection{Mechanizm nasłuchiwania kolejki}

Na listingu~\ref{lst:queue_listener} przedstawiono implementację procedury odpowiedzialnej za rejestrację i obsługę potoku zdarzeń pochodzących z kolejki chmurowej. Logika biznesowa realizuje bezpieczne parsowanie komunikatów w formacie JSON oraz obsługuje scenariusz awaryjny (ang. \textit{dead-lettering/abandonment}) poprzez wywołanie metody \texttt{abandonMessage()}, co pozwala na zachowanie spójności danych w przypadku wystąpienia błędów infrastrukturalnych.

\begin{lstlisting}[language=JavaScript,caption={Implementacja mechanizmu nasłuchiwania kolejki (\texttt{printQueueListener.js})},style=jsStyle,label={lst:queue_listener}]
export function startPrintQueueListener(container, receiver) {
	if (!container) throw new Error('container is required for printQueueListener');
	if (!receiver) throw new Error('receiver is required for printQueueListener');
	
	const messageHandler = async (message) => {
		try {
			let data = message.body;
			if (typeof data === 'string') {
				try { 
					data = JSON.parse(data); 
				} catch(e) { 
					await receiver.abandonMessage(message); 
					return; 
				}
			}
			
			if (!data || data.event !== 'file_uploaded') {
				return;
			}
			
			const job = {
				id: Date.now().toString(),
				fileId: data.fileId,
				originalFileName: data.originalFileName || null,
				userId: data.userId,
				userEmail: data.userEmail,
				status: data.scheduledAt ? 'scheduled' : 'pending',
				scheduledAt: data.scheduledAt || null,
				createdAt: getLocalISO(),
			};
			
			try {
				await container.items.upsert(job);
			} catch(upErr) {
				throw upErr;
			}
		} catch(err) {
			try { 
				await receiver.abandonMessage(message); 
			} catch(e) { 
				// Obsluga bledu komunikacji z repozytorium kolejki
			}
		}
	};
	
	const errorHandler = (error) => {
		console.error('EVENT ERROR:', error);
	};
	
	receiver.subscribe({ 
		processMessage: messageHandler, 
		processError: errorHandler 
	});
}
\end{lstlisting}

\subsubsection{Punkty końcowe HTTP API}

W tabeli~\ref{tab:queue_endpoints} zestawiono punkty końcowe udostępniane przez interfejs sieciowy usługi \textit{Queue Service}.

\begin{table}[htbp]
	\centering
	\caption{Punkty końcowe usługi Queue Service.}
	\label{tab:queue_endpoints}
	\begin{tabular}{llp{7cm}}
		\toprule
		\textbf{Metoda} & \textbf{Ścieżka} & \textbf{Opis} \\
		\midrule
		GET & \texttt{/queue} & Pobranie spaginowanej listy zadań z możliwością definiowania filtrów wyszukiwania. \\
		\addlinespace
		GET & \texttt{/queue/next} & Pobranie kolejnego zadania przeznaczonego bezpośrednio do realizacji (zwraca kod \texttt{204 No Content} w przypadku braku pozycji). \\
		\addlinespace
		PATCH & \texttt{/queue/cancel/:id} & Anulowanie wybranego zadania drukowania (dostęp ograniczony do roli administratora). \\
		\addlinespace
		GET & \texttt{/queues} & Pobranie metryk i informacji o stanie kolejek w ramach usługi \textit{Azure Service Bus} (dostęp dla administratora). \\
		\addlinespace
		GET & \texttt{/health} & Punkt końcowy diagnostyki stanu działania mikroserwisu (ang. \textit{health check}). \\
		\bottomrule
	\end{tabular}
\end{table}


\subsection{Printer Service}

\subsubsection{Opis ogólny}

\subsubsection{Harmonogram}

\subsubsection{Monitor gotowości}

\subsubsection{Listener telemetrii}

\subsubsection{Enpointy HTTP API}


\subsection{Video Service}

\newpage

% ============================================================
%  ROZDZIAŁ 6 — FRONTEND
% ============================================================
\section{Implementacja frontendu}
\label{sec:frontend}

\subsection{Struktura aplikacji}

\subsection{Klient API}

\subsection{Routing i ochrona tras}

\subsection{Ekrany aplikacji}

\subsubsection{Strona główna}

\subsubsection{Dashboard użytkownika}

\subsubsection{Upload i planowanie}

\subsubsection{Kontrola druku}

\subsubsection{Panel administracyjny}


\subsection{Internacjonalizacja}

\subsection{Obsługa motywu}

\newpage

% ============================================================
%  ROZDZIAŁ 7 — AGENT RASPBERRY PI
% ============================================================
\section{Agent Raspberry Pi - AddiPi Agent}
\label{sec:agent}

\subsection{Opis ogólny}

\subsection{Architektura agenta}

\subsection{Połączenie z IoT Hub}

\subsection{Obsługiwane metody bezpośrednie}

\subsection{Przebieg obsługi metody startPrint}

\subsection{Telemetria}

\subsection{Komunikacja z OctoPrint}

\newpage

% ============================================================
%  ROZDZIAŁ 8 — OBUDOWA CAD
% ============================================================
\section{Obudowa urządzenia - projekt CAD}
\label{sec:cad}

\subsection{Cel i zakres projektu mechanicznego}

\subsection{Moduł CASE - obudowa Raspberry Pi z kamerą}

\subsubsection{Podstawa projektowa}

\subsubsection{Zakres modyfikacji}

\subsubsection{Mocowanie kamery}

\subsubsection{Struktura plików CASE}


\subsection{Moduł STAND - podstawka regulowana}

\subsection{Złożenie całości}

\subsection{Parametry wydruku}

\subsection{Instrukcja montażu}

\newpage

% ============================================================
%  ROZDZIAŁ 9 — KONFIGURACJA RASPBERRY PI
% ============================================================
\section{Konfiguracja Raspberry Pi}
\label{sec:rpi}

\subsection{Opis środowiska}

\subsection{Instalacja agenta}

\subsection{Automatyczne uruchamianie agenta przez systemd}

\subsection{Tunel Cloudflare - ekspozycja kamery}

\subsubsection{Instalacja cloudflared}

\subsubsection{Skrypt publikowania URL kamery}

\subsubsection{Cykliczne odnawianie URL}


\subsection{Konfiguracja crontab}

\subsubsection{Uzasadnienie parametrów}


\subsection{Problem z dostępnością sieci przy starcie}

\subsection{Podsumowanie konfiguracji Raspberry Pi}

\newpage

% ============================================================
%  ROZDZIAŁ 10 — WDROŻENIE I INFRASTRUKTURA
% ============================================================
\section{Wdrożenie i infrastruktura}
\label{sec:wdrozenie}

\subsection{Infrastruktura chmurowa}

\subsection{Inicjalizacja infrastruktury}

\subsection{Konteneryzacja - Dockerfiles}

\subsection{Docker Compose}

\subsection{Zmienne środowiskowe}

\subsection{Wdrożenie frontendu}

\subsection{Wdrożenie agenta}

\newpage

% ============================================================
%  ROZDZIAŁ 11 — TESTY
% ============================================================
\section{Testowanie systemu}
\label{sec:testy}

\subsection{Metodologia testowania}

\subsection{Testy jednostkowe - Auth Service}

\subsection{Testy integracyjne przez Postman}

\subsection{Test end-to-end - pełny przepływ druku}

\subsection{Testy wydajnościowe i obserwacje}

\newpage

% ============================================================
%  ROZDZIAŁ 12 — PODSUMOWANIE
% ============================================================
\section{Podsumowanie}
\label{sec:podsumowanie}

\subsection{Osiągnięcia}

\subsection{Napotkane trudności}

\subsection{Możliwości rozwoju}

\subsection{Wnioski}



\newpage

% ============================================================
%  BIBLIOGRAFIA
% ============================================================

\bibliographystyle{plain}
\bibliography{bibliografia}\textsc{}

\newpage

% ============================================================
%  DODATKI
% ============================================================

\begin{appendices}

\section{Konfiguracja środowiska deweloperskiego}

\subsection{Wymagania}

\subsection{Pierwsze uruchomienie}

\section{Struktura repozytoriów}

\section{Endpointy API - podsumowanie}

\end{appendices}

\end{document}




